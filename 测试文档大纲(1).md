# 股票交易系统管理子系统 (ADMIN) — 测试文档大纲

> **版本**: v1.0.0  
> **日期**: 2026-06-05  
> **对应项目**: the_stock_trade_system-ADMIN  
> **技术栈**: FastAPI + Vue 3 (Element Plus) + MySQL + JWT

---

## 目录

1. [概述](#1-概述)
2. [测试范围](#2-测试范围)
3. [测试环境](#3-测试环境)
4. [测试策略](#4-测试策略)
5. [功能测试用例](#5-功能测试用例)
   - [5.1 认证模块 (Auth)](#51-认证模块-auth)
   - [5.2 股票查看模块 (Stocks)](#52-股票查看模块-stocks)
   - [5.3 涨跌停设置模块 (Limits)](#53-涨跌停设置模块-limits)
   - [5.4 交易控制模块 (Trade Control)](#54-交易控制模块-trade-control)
   - [5.5 交易日管理模块 (Trading Days)](#55-交易日管理模块-trading-days)
   - [5.6 权限管理模块 (Permissions)](#56-权限管理模块-permissions)
   - [5.7 审计日志模块 (Audit)](#57-审计日志模块-audit)
   - [5.8 健康检查 (Health Check)](#58-健康检查-health-check)
6. [API 集成测试](#6-api-集成测试)
7. [前端 / E2E 测试](#7-前端--e2e-测试)
8. [安全测试](#8-安全测试)
9. [性能测试](#9-性能测试)
10. [测试交付物](#10-测试交付物)

---

## 1. 概述

### 1.1 系统简介

股票交易系统管理子系统 (ADMIN) 是一个基于 **FastAPI** 的后台管理系统，为管理员提供股票交易相关的管理功能，包括管理员认证、股票行情查看、涨跌停比例设置、交易暂停/重启、交易日管理、管理员权限管理以及审计日志查询与导出。

### 1.2 架构概览

```
┌──────────────────────┐     HTTP/httpx      ┌──────────────────────┐
│   ADMIN 子系统        │ ◄──────────────────► │   TRADE 交易核心      │
│   (FastAPI + MySQL)   │                      │   (独立服务)          │
└──────────┬───────────┘                      └──────────────────────┘
           │
    ┌──────┴──────┐
    │  Vue 3 前端  │  (Element Plus)
    └─────────────┘
```

### 1.3 关键数据模型

| 模型 | 表名 | 说明 |
|------|------|------|
| Admin | `admin_info` | 管理员账户 |
| PermissionConfig | `permission_config` | 管理员股票授权范围 |
| LoginLog | `login_log` | 登录/登出日志 |
| OperationLog | `operation_log` | 操作审计日志 |

### 1.4 管理员角色体系

| 角色 | 编码 | 说明 |
|------|------|------|
| NORMAL_ADMIN | 0 | 普通管理员（受限股票范围） |
| SENIOR_ADMIN | 1 | 高级管理员（涨跌停/交易控制/交易日） |
| SYSTEM_ADMIN | 2 | 系统管理员（权限管理 + 全部） |
| AUDIT_ADMIN | 3 | 审计管理员（只读审计日志） |

---

## 2. 测试范围

### 2.1 包含范围

- 所有后端 REST API 接口的功能正确性
- 数据库模型 (SQLAlchemy ORM) 的 CRUD 操作
- JWT 认证与中间件鉴权逻辑
- 角色权限控制 (RBAC) 边界
- 管理员账户锁定/解锁机制
- 与 TRADE 服务的 HTTP 客户端集成（含超时、异常处理）
- 前端页面渲染、交互、路由守卫、API 调用
- 数据校验（Pydantic Schema 验证）
- CSV 导出功能
- 全局异常处理与统一响应格式

### 2.2 不包含范围

- TRADE 交易核心内部逻辑（属于独立子系统）
- 股票撮合引擎算法
- MySQL 数据库本身的性能调优
- 生产环境部署与运维监控
- 第三方支付/实名认证集成

---

## 3. 测试环境

### 3.1 推荐测试拓扑

```
┌────────────┐     ┌──────────────┐     ┌────────────┐
│ 测试客户端   │ ──► │ ADMIN 服务    │ ──► │ MySQL 测试  │
│ (curl/py)  │     │ localhost:8000│     │ 实例        │
└────────────┘     └──────┬───────┘     └────────────┘
                          │ (Mock 或真实)
                          ▼
                   ┌──────────────┐
                   │ TRADE Mock   │
                   │ 服务          │
                   └──────────────┘
```

### 3.2 依赖与配置

| 组件 | 版本要求 | 说明 |
|------|----------|------|
| Python | ≥ 3.10 | 后端运行环境 |
| Node.js | ≥ 18 | 前端构建环境 |
| MySQL | ≥ 8.0 | 数据持久化 |
| FastAPI | ≥ 0.110.0 | Web 框架 |
| Vue 3 | 最新稳定版 | 前端框架 |
| Element Plus | 最新稳定版 | UI 组件库 |

### 3.3 测试数据

- 使用 `seed.py` 预置的四类管理员账户:
  - `normal_admin` / `Admin@123` (NORMAL_ADMIN)
  - `senior_admin` / `Admin@123` (SENIOR_ADMIN)
  - `sys_admin` / `Admin@123` (SYSTEM_ADMIN)
  - `audit_admin` / `Admin@123` (AUDIT_ADMIN)

---

## 4. 测试策略

### 4.1 测试层次

```
     ┌────────────────┐
     │  E2E 测试       │  ← Playwright / Cypress（少量、冒烟级别）
     ├────────────────┤
     │  集成测试        │  ← pytest + httpx（API 级别，核心）
     ├────────────────┤
     │  单元测试        │  ← pytest（业务逻辑层、工具函数）
     └────────────────┘
```

### 4.2 测试优先级

| 优先级 | 模块 | 理由 |
|--------|------|------|
| P0 | Auth (登录/鉴权) | 所有接口的前置依赖 |
| P0 | 角色权限控制 | 安全核心 |
| P1 | 涨跌停设置 | 交易核心管理功能 |
| P1 | 交易控制 (暂停/重启) | 交易核心管理功能 |
| P1 | 交易日管理 | 交易核心管理功能 |
| P2 | 股票查看 | 基础展示功能 |
| P2 | 权限管理 | 管理员运维功能 |
| P2 | 审计日志 | 审计与追溯功能 |
| P3 | 健康检查 | 运维监控辅助 |

### 4.3 测试类型

- **功能测试 (90%+)**: 手动 + 自动化，覆盖所有 API 端点
- **边界测试**: 输入边界、权限边界、状态边界
- **异常测试**: 网络异常、数据库异常、上游服务异常
- **安全测试**: 未授权访问、越权操作、Token 伪造/过期
- **性能测试**: 并发登录、日志大数据量查询、CSV 导出

---

## 5. 功能测试用例

### 5.1 认证模块 (Auth)

**API 端点**:

| 方法 | 路径 | 说明 | 鉴权 |
|------|------|------|------|
| POST | `/api/v1/admin/auth/login` | 管理员登录 | 无 |
| POST | `/api/v1/admin/auth/logout` | 管理员登出 | Bearer Token |
| POST | `/api/v1/admin/auth/password` | 修改密码 | Bearer Token |
| GET | `/api/v1/admin/auth/me` | 获取当前管理员信息 | Bearer Token |

**测试用例清单**:

| 编号 | 用例名称 | 前置条件 | 输入/步骤 | 预期结果 |
|------|----------|----------|-----------|----------|
| AUTH-001 | 正常登录 | 数据库有 seed 账号 | `POST` normal_admin / Admin@123 | 返回 200, token 非空, role=NORMAL_ADMIN |
| AUTH-002 | 用户名不存在 | 干净数据库 | `POST` 不存在的用户名 | 返回 401 "用户名或密码错误" |
| AUTH-003 | 密码错误 | 已有账号 | `POST` 正确用户名 + 错误密码 | 返回 401，剩余次数递减 |
| AUTH-004 | 账户锁定机制 | 已有账号 | 连续 5 次错误密码 | 第 6 次返回 403 "账户已锁定，剩余X分X秒" |
| AUTH-005 | 锁定后自动解锁 | 已锁定账户 | 等待锁定时间 (5分钟) 后登录 | 自动解锁，登录成功 |
| AUTH-006 | 禁用账户登录 | 账户状态=disabled | 使用被禁用账户登录 | 返回 403 "账户已被禁用" |
| AUTH-007 | 注销登录 | 已有有效 token | `POST /auth/logout` | 返回 200 "已退出登录"，记录登出时间 |
| AUTH-008 | 无 Token 访问受保护接口 | 无 Token | `GET /auth/me` 不带 Authorization | 返回 401 "未登录或令牌无效" |
| AUTH-009 | 伪造 Token | 已获取 token | 使用篡改过的 JWT | 返回 401 "令牌无效或已过期" |
| AUTH-010 | Token 版本失效 | 修改密码后 | 使用旧 token 访问 | 返回 401 "令牌已失效，请重新登录" |
| AUTH-011 | 修改密码成功 | 已登录 | 原密码正确 + 新密码符合强度 | 返回 200 "密码修改成功" |
| AUTH-012 | 修改密码-原密码错误 | 已登录 | 原密码错误 | 返回 401 "原密码错误" |
| AUTH-013 | 修改密码-两次不一致 | 已登录 | 新密码 ≠ 确认密码 | 返回错误 "两次输入的新密码不一致" |
| AUTH-014 | 修改密码-强度不足 | 已登录 | 密码仅含数字 (`12345678`) | 返回错误 "需包含...至少三类" |
| AUTH-015 | 修改密码-弱密码 | 已登录 | 长度 < 8 | 返回错误 "密码长度不能少于8位" |
| AUTH-016 | 获取当前用户信息 | 已登录 | `GET /auth/me` | 返回 admin_id, username, role, status |
| AUTH-017 | 登录日志记录 | 任意登录 | 多次登录/失败 | login_log 表新增记录，字段完整 |

---

### 5.2 股票查看模块 (Stocks)

**API 端点**:

| 方法 | 路径 | 说明 | 鉴权 |
|------|------|------|------|
| GET | `/api/v1/admin/stocks` | 股票列表（支持搜索） | Bearer Token |
| GET | `/api/v1/admin/stocks/{code}/quote` | 单只股票行情 | Bearer Token |
| GET | `/api/v1/admin/stocks/{code}/order-book` | 委托簿（买卖盘口） | Bearer Token |

**测试用例清单**:

| 编号 | 用例名称 | 前置条件 | 输入/步骤 | 预期结果 |
|------|----------|----------|-----------|----------|
| STOCK-001 | 普通管理员查看授权股票列表 | NORMAL_ADMIN 登录，授权 600000,000001,600036 | `GET /stocks` | 仅返回授权范围内的股票 |
| STOCK-002 | 高级管理员查看全部股票 | SENIOR_ADMIN 登录 | `GET /stocks` | 返回全部股票（无授权限制） |
| STOCK-003 | 关键词搜索 | 已有数据 | `GET /stocks?keyword=600` | 返回代码或名称匹配 "600" 的股票 |
| STOCK-004 | 空关键词 | 已有数据 | `GET /stocks?keyword=` | 返回所有授权股票 |
| STOCK-005 | 查看单只股票行情 | 授权范围内 | `GET /stocks/600000/quote` | 返回行情对象: latest_price, open_price, high, low, volume 等 |
| STOCK-006 | 查看未授权股票行情 | NORMAL_ADMIN 无该股票权限 | `GET /stocks/999999/quote` | 返回错误 "管理员无该股票权限" |
| STOCK-007 | 查看不存在的股票行情 | 任意管理员 | `GET /stocks/XXXXXX/quote` | TRADE 返回 404 → ADMIN 返回 "股票不存在或行情不可用" |
| STOCK-008 | 查看委托簿 | 任意管理员 | `GET /stocks/600000/order-book` | 返回 buy_levels[] 和 sell_levels[] |
| STOCK-009 | TRADE 服务不可达 | 停止 TRADE 服务 | 任意股票接口 | 返回错误码 `ADMIN_UPSTREAM_ERROR` |
| STOCK-010 | TRADE 服务超时 | 模拟 TRADE 超时 | 任意股票接口 | 返回 "TRADE服务超时" |

---

### 5.3 涨跌停设置模块 (Limits)

**API 端点**:

| 方法 | 路径 | 说明 | 鉴权 |
|------|------|------|------|
| PUT | `/api/v1/admin/stocks/{code}/limits` | 设置涨跌停比例 | Bearer Token (SENIOR_ADMIN) |

**测试用例清单**:

| 编号 | 用例名称 | 前置条件 | 输入/步骤 | 预期结果 |
|------|----------|----------|-----------|----------|
| LIMIT-001 | 高级管理员设置涨跌停 | SENIOR_ADMIN 登录，有权限 | `PUT` limit_up=0.10, limit_down=0.10 | 返回 200，包含涨停价、跌停价 |
| LIMIT-002 | 普通管理员无权设置 | NORMAL_ADMIN 登录 | `PUT /stocks/600000/limits` | 返回 403 "仅高级管理员可设置涨跌停" |
| LIMIT-003 | 审计管理员无权设置 | AUDIT_ADMIN 登录 | `PUT /stocks/600000/limits` | 返回 403 |
| LIMIT-004 | 涨幅为 0 | SENIOR_ADMIN | limit_up_ratio=0 | 返回错误 "涨跌停比例需在(0, 0.10]范围内" |
| LIMIT-005 | 涨幅超过 0.10 | SENIOR_ADMIN | limit_up_ratio=0.15 | 返回错误 |
| LIMIT-006 | 跌幅为 0 | SENIOR_ADMIN | limit_down_ratio=0 | 返回错误 |
| LIMIT-007 | 格式无效 | SENIOR_ADMIN | limit_up_ratio="abc" | 返回错误 "涨跌停比例格式无效" |
| LIMIT-008 | 设置未授权股票 | SENIOR_ADMIN 无该股票权限 | 无权限的股票代码 | 返回错误 "管理员无该股票权限" |
| LIMIT-009 | 指定生效日期 | SENIOR_ADMIN | effective_date="2026-06-10" | 设置成功，携带生效日期 |
| LIMIT-010 | 设置后审计日志记录 | SENIOR_ADMIN | 成功设置 | operation_log 新增一条 LIMIT_SET 记录 |

---

### 5.4 交易控制模块 (Trade Control)

**API 端点**:

| 方法 | 路径 | 说明 | 鉴权 |
|------|------|------|------|
| POST | `/api/v1/admin/stocks/{code}/pause` | 暂停股票交易 | Bearer Token (SENIOR_ADMIN) |
| POST | `/api/v1/admin/stocks/{code}/resume` | 重启股票交易 | Bearer Token (SENIOR_ADMIN) |

**测试用例清单**:

| 编号 | 用例名称 | 前置条件 | 输入/步骤 | 预期结果 |
|------|----------|----------|-----------|----------|
| CTRL-001 | 高级管理员暂停交易 | SENIOR_ADMIN 登录 | `POST /stocks/600000/pause` + 原因 | 返回 200 "股票600000已暂停交易" |
| CTRL-002 | 高级管理员重启交易 | 股票已暂停 | `POST /stocks/600000/resume` + 原因 | 返回 200 "股票600000已重启交易" |
| CTRL-003 | 普通管理员无权暂停 | NORMAL_ADMIN | `POST /stocks/600000/pause` | 返回 403 "仅高级管理员可暂停交易" |
| CTRL-004 | 普通管理员无权重启 | NORMAL_ADMIN | `POST /stocks/600000/resume` | 返回 403 "仅高级管理员可重启交易" |
| CTRL-005 | 暂停已暂停的股票 | 股票已暂停 | 再次暂停 | 由 TRADE 返回相应错误 |
| CTRL-006 | 重启未暂停的股票 | 股票正常交易 | 直接重启 | 由 TRADE 返回相应错误 |
| CTRL-007 | 暂停时审计日志记录 | SENIOR_ADMIN | 暂停操作完成 | operation_log 新增 TRADE_CONTROL 记录 |
| CTRL-008 | TRADE 不可达 | 停止 TRADE | 暂停/重启操作 | 返回 ADMIN_UPSTREAM_ERROR |

---

### 5.5 交易日管理模块 (Trading Days)

**API 端点**:

| 方法 | 路径 | 说明 | 鉴权 |
|------|------|------|------|
| POST | `/api/v1/admin/trading-days/open` | 交易日开盘 | Bearer Token (SENIOR_ADMIN) |
| POST | `/api/v1/admin/trading-days/close` | 交易日收盘 | Bearer Token (SENIOR_ADMIN) |

**测试用例清单**:

| 编号 | 用例名称 | 前置条件 | 输入/步骤 | 预期结果 |
|------|----------|----------|-----------|----------|
| TD-001 | 高级管理员开启交易日 | SENIOR_ADMIN 登录 | `POST /trading-days/open` + trade_date | 返回 200，交易日开始（启动撮合引擎） |
| TD-002 | 高级管理员结束交易日 | 交易日进行中 | `POST /trading-days/close` + 原因 | 返回 200，停止接收新指令、过期未成交指令 |
| TD-003 | 普通管理员无权操作 | NORMAL_ADMIN | 开/收盘 | 返回 403 "仅高级管理员可管理交易日" |
| TD-004 | 缺少日期参数 | SENIOR_ADMIN | trade_date 为空 | Schema 校验失败 |
| TD-005 | 重复开盘 | 当日已开盘 | 再次开盘 | 由 TRADE 返回相应错误 |
| TD-006 | 交易日操作审计日志 | SENIOR_ADMIN | 开/收盘 | operation_log 新增 TRADING_DAY 记录 |

---

### 5.6 权限管理模块 (Permissions)

**API 端点**:

| 方法 | 路径 | 说明 | 鉴权 |
|------|------|------|------|
| GET | `/api/v1/admin/admins` | 获取所有管理员列表 | Bearer Token (SYSTEM_ADMIN) |
| PUT | `/api/v1/admin/admins/{id}/permissions` | 修改管理员权限 | Bearer Token (SYSTEM_ADMIN) |

**测试用例清单**:

| 编号 | 用例名称 | 前置条件 | 输入/步骤 | 预期结果 |
|------|----------|----------|-----------|----------|
| PERM-001 | 系统管理员查看所有管理员 | SYSTEM_ADMIN 登录 | `GET /admins` | 返回所有管理员信息，含角色、状态、授权股票 |
| PERM-002 | 非系统管理员无权查看 | NORMAL_ADMIN 登录 | `GET /admins` | 返回 403 |
| PERM-003 | 修改管理员角色 | SYSTEM_ADMIN | `PUT /admins/1/permissions` role=NORMAL→SENIOR | 返回 200 "权限更新成功" |
| PERM-004 | 修改管理员状态 | SYSTEM_ADMIN | `PUT` status=active→disabled | 返回 200，被禁用账户 token_version+1 |
| PERM-005 | 修改股票授权范围 | SYSTEM_ADMIN | `PUT` authorized_stocks=["600000","000001"] | 返回 200 |
| PERM-006 | 目标管理员不存在 | SYSTEM_ADMIN | `PUT /admins/999/permissions` | 返回 404 "目标管理员不存在" |
| PERM-007 | 无效角色 | SYSTEM_ADMIN | role="INVALID_ROLE" | 返回错误 "无效角色" |
| PERM-008 | 无效状态 | SYSTEM_ADMIN | status="unknown" | 返回错误 "无效状态" |
| PERM-009 | 修改自身权限 | SYSTEM_ADMIN | 修改自己的权限 | 应允许（或根据业务需求决定） |
| PERM-010 | 操作审计记录 | SYSTEM_ADMIN | 任何权限变更 | operation_log 新增 PERMISSION 记录 |

---

### 5.7 审计日志模块 (Audit)

**API 端点**:

| 方法 | 路径 | 说明 | 鉴权 |
|------|------|------|------|
| GET | `/api/v1/admin/audit/operation-logs` | 查询操作日志 | Bearer Token (AUDIT/SYSTEM) |
| GET | `/api/v1/admin/audit/login-logs` | 查询登录日志 | Bearer Token (AUDIT/SYSTEM) |
| GET | `/api/v1/admin/audit/logs/export` | 导出日志 CSV | Bearer Token (AUDIT/SYSTEM) |
| DELETE | `/api/v1/admin/audit/logs` | 删除日志 | Bearer Token (AUDIT/SYSTEM) |

**测试用例清单**:

| 编号 | 用例名称 | 前置条件 | 输入/步骤 | 预期结果 |
|------|----------|----------|-----------|----------|
| AUDIT-001 | 审计管理员查询操作日志 | AUDIT_ADMIN 登录 | `GET /audit/operation-logs` | 返回分页日志列表 |
| AUDIT-002 | 系统管理员查询操作日志 | SYSTEM_ADMIN 登录 | 同上 | 成功 |
| AUDIT-003 | 普通/高级管理员无权 | NORMAL_ADMIN / SENIOR_ADMIN | 查询/导出/删除 | 返回 403 |
| AUDIT-004 | 按管理员 ID 筛选 | 有多条日志 | `?admin_id=1` | 仅返回该管理员的操作日志 |
| AUDIT-005 | 按操作类型筛选 | 有多条日志 | `?operation_type=LIMIT_SET` | 仅返回涨跌停设置相关日志 |
| AUDIT-006 | 按时间范围筛选 | 有多条日志 | `?start_time=...&end_time=...` | 仅返回时间范围内的日志 |
| AUDIT-007 | 分页功能 | 超过 20 条日志 | `?page=2&page_size=10` | 返回第 2 页，每页 10 条 |
| AUDIT-008 | 分页边界 | 数据不足一页 | page=1, page_size=100 | 正常返回（page_size 上限 100） |
| AUDIT-009 | 查询登录日志 | AUDIT_ADMIN | `GET /audit/login-logs` | 返回登录日志列表，含登录/登出时间 |
| AUDIT-010 | 导出操作日志 CSV | 有数据 | `GET /audit/logs/export?log_type=operation` | 返回 CSV 文件，字段完整 |
| AUDIT-011 | 导出登录日志 CSV | 有数据 | `GET /audit/logs/export?log_type=login` | 返回 CSV 文件 |
| AUDIT-012 | 导出时筛选条件 | 有多条数据 | 带 admin_id + 时间参数导出 | 仅导出符合条件的数据 |
| AUDIT-013 | 删除操作日志 | 有数据 | `DELETE /audit/logs?log_type=operation` | 返回 "已删除N条operation日志" |
| AUDIT-014 | 按日期删除 | 有数据 | `DELETE /audit/logs?log_type=login&before=2026-01-01` | 仅删除指定日期之前的日志 |
| AUDIT-015 | 日期格式无效 | | before="abc" | 返回错误 "日期格式无效" |
| AUDIT-016 | log_type 参数无效 | | log_type=invalid | 422 (pattern 校验失败) |

---

### 5.8 健康检查 (Health Check)

**API 端点**:

| 方法 | 路径 | 说明 | 鉴权 |
|------|------|------|------|
| GET | `/health` | 服务健康检查 | 无 |

**测试用例清单**:

| 编号 | 用例名称 | 前置条件 | 输入/步骤 | 预期结果 |
|------|----------|----------|-----------|----------|
| HC-001 | 全部正常 | MySQL + TRADE 均可用 | `GET /health` | 200, status=UP, mysql=UP, trade=UP |
| HC-002 | MySQL 不可用 | 停止 MySQL | `GET /health` | 503, status=DEGRADED, mysql=DOWN |
| HC-003 | TRADE 不可用 | 停止 TRADE | `GET /health` | 503, status=DEGRADED, trade=DOWN |
| HC-004 | 两者都不可用 | 全部停止 | `GET /health` | 503, status=DEGRADED |

---

## 6. API 集成测试

### 6.1 统一响应格式

所有 API 应遵循统一响应格式:

```json
{
  "success": true | false,
  "code": "OK" | "ERROR_CODE",
  "message": "...",
  "data": {...},
  "request_id": null,
  "timestamp": "2026-06-05T..."
}
```

### 6.2 推荐的 API 测试脚本结构 (pytest)

```
tests/
├── conftest.py              # fixtures: db session, test client, auth headers
├── test_auth.py             # AUTH-001 ~ AUTH-017
├── test_stocks.py           # STOCK-001 ~ STOCK-010
├── test_limits.py           # LIMIT-001 ~ LIMIT-010
├── test_trade_control.py    # CTRL-001 ~ CTRL-008
├── test_trading_days.py     # TD-001 ~ TD-006
├── test_permissions.py      # PERM-001 ~ PERM-010
├── test_audit.py            # AUDIT-001 ~ AUDIT-016
├── test_health.py           # HC-001 ~ HC-004
└── factories/               # 测试数据工厂
    ├── admin_factory.py
    └── log_factory.py
```

### 6.3 关键 Test Fixture 设计

| Fixture | 作用 |
|---------|------|
| `client` | FastAPI TestClient (app) |
| `db_session` | 独立测试数据库会话（每次测试后回滚） |
| `normal_token` | 预登录的 NORMAL_ADMIN JWT |
| `senior_token` | 预登录的 SENIOR_ADMIN JWT |
| `system_token` | 预登录的 SYSTEM_ADMIN JWT |
| `audit_token` | 预登录的 AUDIT_ADMIN JWT |
| `mock_trade_client` | Mock TradeClient 以隔离 TRADE 依赖 |

### 6.4 TradeClient Mock 要点

TRADE 服务为独立外部依赖，集成测试时建议 Mock `TradeClient`:

- 正常返回行情数据
- 模拟 `TradeClientError` 异常
- 模拟超时 (`httpx.TimeoutException`)
- 模拟连接失败 (`httpx.ConnectError`)
- 验证 ADMIN → TRADE 请求参数转换正确性（如 `operator_admin_id` 格式化为 `ADM000001`）

---

## 7. 前端 / E2E 测试

### 7.1 页面清单

| 路由 | 页面 | 所需角色 |
|------|------|----------|
| `/login` | 登录页 | 无 |
| `/stocks` | 股票查看 | 全部角色 |
| `/limits` | 涨跌停设置 | SENIOR_ADMIN |
| `/trade-control` | 交易控制 | SENIOR_ADMIN |
| `/permissions` | 权限管理 | SYSTEM_ADMIN |
| `/audit` | 审计日志 | AUDIT_ADMIN, SYSTEM_ADMIN |
| `/password` | 密码修改 | 全部角色（登录后） |

### 7.2 前端测试要点

| 编号 | 测试项 | 说明 |
|------|--------|------|
| FE-001 | 路由守卫 | 未登录访问 /stocks → 自动跳转 /login |
| FE-002 | 角色路由守卫 | NORMAL_ADMIN 访问 /permissions → 重定向 /stocks |
| FE-003 | Token 过期处理 | API 返回 401 → 自动清除 localStorage 并跳转登录 |
| FE-004 | 登录表单校验 | 用户名为空 → 提示 "请输入用户名" |
| FE-005 | 登录成功跳转 | 登录成功 → 跳转 /stocks |
| FE-006 | 登录失败提示 | 密码错误 → 显示错误消息 |
| FE-007 | 股票列表加载 | 进入 /stocks 自动加载列表 |
| FE-008 | 搜索功能 | 输入关键词 → 实时过滤 |
| FE-009 | 股票详情弹窗 | 点击详情 → 显示行情描述列表 |
| FE-010 | 涨跌停表单校验 | 未输入股票代码 → 提示 |
| FE-011 | 交易控制二次确认 | 结束交易日 → 弹出确认框 |
| FE-012 | 审计日志分页 | 点击分页 → 加载对应页 |
| FE-013 | CSV 导出下载 | 点击导出 → 浏览器触发下载 |
| FE-014 | 权限管理编辑 | 编辑角色/状态/授权 → 保存生效 |
| FE-015 | 密码修改表单 | 确认密码不一致 → 校验失败提示 |
| FE-016 | Loading 状态 | 各页面数据加载中显示 loading 动画 |
| FE-017 | 空数据状态 | 无数据时显示提示文字 |
| FE-018 | API 网络异常 | API 不可达 → 错误提示不崩溃 |

### 7.3 推荐 E2E 工具

- **Playwright** 或 **Cypress**
- 覆盖范围：冒烟级别（核心登录→关键操作→登出流程）

---

## 8. 安全测试

### 8.1 认证安全

| 编号 | 测试项 | 方法 |
|------|--------|------|
| SEC-001 | 密码哈希存储 | 确认数据库存储 bcrypt 哈希，非明文 |
| SEC-002 | JWT 签名验证 | 使用错误密钥签名的 token 应被拒绝 |
| SEC-003 | Token 过期 | 设置短过期时间 (如 1min)，过期后请求返回 401 |
| SEC-004 | Token Version 作废 | 密码修改后旧 token 不可用 |
| SEC-005 | 暴力破解防护 | 5 次失败后锁定 5 分钟 |
| SEC-006 | 密码强度策略 | 8 位 + 3/4 类字符组合强制 |
| SEC-007 | SQL 注入 | 在登录用户名/搜索关键词中注入 SQL 语句 |
| SEC-008 | XSS | 在输入字段注入 `<script>` 标签 |

### 8.2 授权安全

| 编号 | 测试项 | 方法 |
|------|--------|------|
| SEC-009 | 水平越权 | NORMAL_ADMIN 尝试访问未授权股票 |
| SEC-010 | 垂直越权 | NORMAL_ADMIN 调用 SENIOR 专属接口 |
| SEC-011 | 角色枚举 | 验证 4 种角色权限边界完全隔离 |
| SEC-012 | 直接对象引用 | 尝试通过修改 URL 参数访问其他管理员的权限 |

---

## 9. 性能测试

### 9.1 性能指标

| 指标 | 目标值 | 说明 |
|------|--------|------|
| 登录接口响应时间 | < 500ms (P95) | 含 bcrypt 验证 |
| 股票列表查询 | < 300ms (P95) | 含 TRADE 代理调用 |
| 审计日志查询 (10万条) | < 1s (P95) | 分页查询含索引 |
| CSV 导出 (10万条) | < 5s | 流式输出 |
| 并发登录 | 50 QPS 无错误 | 连接池 10+20 |

### 9.2 压测场景

| 场景 | 工具 | 并发数 | 持续时间 |
|------|------|--------|----------|
| 登录接口压测 | wrk / locust | 50 | 60s |
| 混合操作压测（查行情+设涨跌停+查日志） | locust | 30 | 120s |
| 大日志量查询 | 手工 | - | 验证 10万条分页响应 |

---

## 10. 测试交付物

### 10.1 交付清单

| 序号 | 交付物 | 格式 | 说明 |
|------|--------|------|------|
| 1 | 测试计划 | .md / .pdf | 本文档 |
| 2 | 测试用例明细 | .xlsx / .csv | 可执行的详细步骤 |
| 3 | 自动化测试代码 | .py (pytest) | 覆盖 P0/P1 用例 |
| 4 | 测试报告 | .md / .pdf | 执行结果、缺陷统计、覆盖率 |
| 5 | 缺陷报告 | 缺陷跟踪系统 | 每个缺陷的详细记录 |
| 6 | 测试数据脚本 | .sql / .py | 可复用的测试数据集 |
| 7 | E2E 测试脚本 | .spec.js / .spec.ts | Playwright/Cypress 脚本 |

### 10.2 测试完成标准

- [ ] P0 用例通过率 100%
- [ ] P1 用例通过率 ≥ 98%
- [ ] P2+P3 用例通过率 ≥ 95%
- [ ] 无 Critical / Blocker 级别缺陷遗留
- [ ] 后端代码覆盖率 ≥ 80%（核心业务逻辑 ≥ 90%）
- [ ] 安全测试全部通过
- [ ] 性能指标达标

---

> **编写**: 基于 `the_stock_trade_system-ADMIN` 源代码完整分析  
> **附录参考**: `2026实验大纲-StockTradingSystem.pdf`, `需求分析报告5.18final.pdf`, `scenario and usecase(1).pdf`
