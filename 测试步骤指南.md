# 测试执行指南（联调前）

> 其他组还没做好，现在只能测你自己写的部分。本文档用最直白的方式告诉你每一步该做什么。

---

## 你需要准备的东西

1. **你的 FastAPI 后端能跑起来**：`uvicorn app.main:app --host 0.0.0.0 --port 8002`
2. **MySQL 数据库已初始化**：跑过 seed.py，四类管理员账号已写入 `admin_info` 表
3. **两个工具**：
   - **浏览器**：打开 `http://localhost:8000/docs`（Swagger UI，点按钮就能发请求）
   - **数据库客户端**（Navicat / DBeaver / MySQL Workbench / 命令行都行）

---

## 第1步：先跑健康检查（最简单，测环境）

> **对应测试用例**：HC-001 ~ HC-004
> **其他组不用参与**：纯测你自己

1. 浏览器打开 `http://localhost:8000/health`
2. 预期看到类似：
   ```json
   {"status":"UP","dependencies":{"mysql":"UP","trade":"DOWN"}}
   ```
3. `mysql=UP` 说明数据库连接正常。`trade=DOWN` 是正常的（别的组没联调）。

✅ 通过条件：MySQL 是 UP，JSON 格式正确。

---

## 第2步：测登录功能（无外部依赖，核心）

> **对应测试用例**：AUTH-001 ~ AUTH-017
> **其他组不用参与**：纯测你自己

### 2.1 正常登录

1. 打开 `http://localhost:8000/docs`，找到 `POST /api/v1/admin/auth/login`
2. 点击 "Try it out"
3. 输入：
   ```json
   {"username": "sys_admin", "password": "Admin@123"}
   ```
4. 点 Execute

✅ 通过条件：
- 返回 200
- `success: true`
- `data.token` 是一长串 JWT（复制下来，后面要用）
- `data.role` 显示你的角色

### 2.2 密码错误

```json
{"username": "sys_admin", "password": "WrongPassword"}
```

✅ 通过条件：返回 401，"用户名或密码错误"

### 2.3 不存在的用户

```json
{"username": "nobody", "password": "whatever"}
```

✅ 通过条件：返回 401

### 2.4 连续输错 5 次，账号被锁定

1. 用 `normal_admin` 账号，填错误密码，连点 Execute 5 次
2. 第 6 次输正确密码 `Admin@123`

✅ 通过条件：第 6 次返回 403，"账户已锁定，剩余 X 分 X 秒"

### 2.5 等 5 分钟后自动解锁

1. 等 5 分钟（或者直接去数据库把 `lock_until` 改成过去的时间）
2. 再用正确密码登录

✅ 通过条件：登录成功

### 2.6 Token 能用吗

1. 用 2.1 拿到的那串 JWT token
2. 找到 `GET /api/v1/admin/auth/me`
3. 点右上角 "Authorize" 按钮，粘贴 token（格式：`Bearer 你的token`）
4. 执行

✅ 通过条件：返回当前用户信息（username, role, status）

---

## 第3步：测密码修改（无外部依赖）

> **对应测试用例**：AUTH-011 ~ AUTH-015 / PWD-001 ~ PWD-003

先登录拿 token，带上 token 调 `POST /api/v1/admin/auth/password`：

### 3.1 正常修改

```json
{
  "old_password": "Admin@123",
  "new_password": "NewPass@5678",
  "confirm_password": "NewPass@5678"
}
```

✅ 通过条件：返回 200，"密码修改成功"

### 3.2 原密码错误

```json
{
  "old_password": "WrongOldPassword",
  "new_password": "NewPass@5678",
  "confirm_password": "NewPass@5678"
}
```

✅ 通过条件：返回 401，"原密码错误"

### 3.3 两次新密码不一致

```json
{
  "old_password": "Admin@123",
  "new_password": "NewPass@5678",
  "confirm_password": "DifferentPass@9999"
}
```

✅ 通过条件：返回 422，提示两次不一致

### 3.4 新密码太弱（全是数字）

```json
{
  "old_password": "Admin@123",
  "new_password": "12345678",
  "confirm_password": "12345678"
}
```

✅ 通过条件：返回 422，提示强度不够（大小写字母/数字/特殊字符至少三类）

### 3.5 密码太短

```json
{
  "old_password": "Admin@123",
  "new_password": "Abc123!",
  "confirm_password": "Abc123!"
}
```

✅ 通过条件：返回 422，提示长度不能少于 8 位

### 3.6 改完密码，旧 token 还能用吗

1. 成功修改密码
2. 用改密前的旧 JWT token 再调一次 `GET /auth/me`

✅ 通过条件：返回 401，旧 token 已失效

---

## 第4步：测权限管理（无外部依赖）

> **对应测试用例**：PERM-001 ~ PERM-010
> **需要 SYSTEM_ADMIN 账号**（`sys_admin`）

先登录 `sys_admin` 拿到 token，带上 token 操作：

### 4.1 查看所有管理员

调用 `GET /api/v1/admin/admins`（带 sys_admin 的 token）

✅ 通过条件：返回管理员列表，每个人有 admin_id, username, role, status, authorized_stocks

### 4.2 非系统管理员能否访问

用 `normal_admin` 的 token 调 `GET /api/v1/admin/admins`

✅ 通过条件：返回 403，提示没有权限

### 4.3 修改某个管理员的角色

调用 `PUT /api/v1/admin/admins/{admin_id}/permissions`（sys_admin token）：
```json
{
  "role": "SENIOR_ADMIN",
  "status": "active",
  "authorized_stocks": ["600000", "000001"]
}
```

然后去数据库查 `admin_info` 表，确认 role 字段已更新。

✅ 通过条件：返回 200，数据库字段正确变更

### 4.4 禁用某个管理员

```json
{
  "role": "SENIOR_ADMIN",
  "status": "disabled",
  "authorized_stocks": ["600000"]
}
```

然后用被禁用的管理员账号登录。

✅ 通过条件：登录返回 403，"账户已被禁用"

---

## 第5步：测审计日志（无外部依赖）

> **对应测试用例**：AUDIT-001 ~ AUDIT-016
> **需要 AUDIT_ADMIN 账号**（`audit_admin`）

前面登录、修改密码、修改权限的操作已经产生了一些日志。现在用 `audit_admin` 登录，拿到 token：

### 5.1 查看操作日志

调用 `GET /api/v1/admin/audit/operation-logs`（audit_admin token）

✅ 通过条件：能看到分页的日志列表，包含刚才做的操作（LOGIN、PASSWORD、PERMISSION 等）

### 5.2 按类型筛选

传参数 `?operation_type=LOGIN`

✅ 通过条件：只返回登录相关的日志

### 5.3 查看登录日志

调用 `GET /api/v1/admin/audit/login-logs`

✅ 通过条件：能看到登录记录，包含 login_time、login_result、ip_address

### 5.4 导出 CSV

调用 `GET /api/v1/admin/audit/logs/export?log_type=operation`

✅ 通过条件：浏览器下载一个 CSV 文件，用 Excel 打开内容完整、不乱码

### 5.5 非审计管理员能否访问

用 `normal_admin` 的 token 调审计接口

✅ 通过条件：返回 403

### 5.6 删除日志

调用 `DELETE /api/v1/admin/audit/logs?log_type=login&before=2026-06-01`

✅ 通过条件：返回 "已删除 N 条日志"，数据库 login_log 表中 2026-06-01 之前的数据被清除

---

## 第5.5步：创建新管理员（无外部依赖）

> **对应测试用例**：PERM-011 ~ PERM-014
> **需要 SYSTEM_ADMIN 账号**（`sys_admin`）

先登录 `sys_admin` 拿到 token。

### 5.5.1 正常创建管理员

调用 `POST /api/v1/admin/admins`（sys_admin token）：
```json
{
  "username": "test_admin",
  "password": "Test@1234",
  "role": "NORMAL_ADMIN",
  "authorized_stocks": ["600000"]
}
```

✅ 通过条件：返回 200，data 含 admin_id, username, role, status。用新密码可立即登录。

### 5.5.2 用户名已存在

```json
{
  "username": "normal_admin",
  "password": "Test@1234",
  "role": "NORMAL_ADMIN"
}
```

✅ 通过条件：返回 422，"用户名已存在"

### 5.5.3 密码太弱

```json
{
  "username": "weak_admin",
  "password": "12345678",
  "role": "NORMAL_ADMIN"
}
```

✅ 通过条件：返回 422，提示密码强度不足

### 5.5.4 非系统管理员无权

用 `normal_admin` 的 token 调 `POST /api/v1/admin/admins`

✅ 通过条件：返回 403

---

## 第5.6步：添加股票（需要 TRADE）

> **对应测试用例**：STOCK-011 ~ STOCK-013
> **需要 SENIOR_ADMIN 账号 + TRADE 运行 + TRADE 有 POST /stocks 接口**

先用 senior_admin 登录拿 token。

### 5.6.1 正常添加股票

调用 `POST /api/v1/admin/stocks`（senior_admin token）：
```json
{
  "stock_code": "600036",
  "stock_name": "招商银行",
  "stock_type": "NORMAL",
  "previous_close_price": "35.00"
}
```

✅ 通过条件：返回 200，data 含涨跌停价。

### 5.6.2 普通管理员无权

用 `normal_admin` 的 token 调 `POST /api/v1/admin/stocks`

✅ 通过条件：返回 403

### 5.6.3 重复代码

对已存在的 stock_code 再次 POST

✅ 通过条件：返回 409 或错误提示

---

## 第6步：验证数据库（不用 API，直接查库）

这些是在联调前你应该自己确认的：

### 6.1 密码是哈希存储的

```sql
SELECT username, password_hash FROM admin_info;
```

✅ 通过条件：password_hash 是一长串乱码（bcrypt 哈希），不是 `Admin@123`

### 6.2 外键约束生效

```sql
DELETE FROM admin_info WHERE admin_id = 1;
```

✅ 通过条件：执行失败，提示外键约束（因为有 operation_log 引用了这个 admin_id）

### 6.3 索引存在

```sql
SHOW INDEX FROM admin_info;
SHOW INDEX FROM operation_log;
SHOW INDEX FROM login_log;
```

✅ 通过条件：username 有唯一索引，admin_id、operation_type、operation_time 有普通索引

---

## 自动化测试（推荐）

已编写 pytest 自动化脚本 `test_suite.py` + `conftest.py`，覆盖大部分可自动化测试的用例。

```bash
cd 测试报告
pip install pytest httpx pymysql

# 运行全部独立测试（无需 TRADE）
DB_PASSWORD=你的MySQL密码 pytest test_suite.py -v -m "not trade"

# 运行 TRADE 依赖测试（联调后）
DB_PASSWORD=你的MySQL密码 pytest test_suite.py -v -m trade

# 运行全部
DB_PASSWORD=你的MySQL密码 pytest test_suite.py -v
```

---

## 你现在测不了的东西（等别的组联调后再说）

| 模块 | 为什么现在测不了 |
|------|-----------------|
| 股票查看（STOCK-xxx） | 需要 TRADE /market 接口返回行情数据 |
| 涨跌停设置（LIMIT-xxx 含TRADE部分） | 需要 TRADE /stocks/{code}/limits 接口 |
| 交易暂停/重启（CTRL-xxx 含TRADE部分） | 需要 TRADE /stocks/{code}/pause、/resume |
| 交易日管理（TD-xxx 含TRADE部分） | 需要 TRADE /trading-days/open、/close |
| 健康检查 TRADE 状态（HC-003/004） | 需要 TRADE 服务运行 |
| 边界值测试中涉及股票比例的部分 | 需要 TRADE |
| 前端所有测试（FE-001 ~ FE-018） | 需要前端启动 |
| 性能/压力测试 | 功能没跑通之前做压测没有意义 |
| E2E 测试 | 需要前端 + 全部后端联调 |

**注意**：涨跌停/交易控制/交易日管理中「权限校验」类的测试（403 返回）不需要 TRADE，自动化脚本已覆盖。

---

## 仍需手动操作的部分

以下测试用例自动化脚本无法覆盖，需手动操作：

### 手动-1：锁定到期自动解锁（AUTH-005 / BND-008）

1. 先触发锁定（连续5次输错 senior_admin 的密码）
2. 两种方式验证解锁：
   - **等5分钟**（推荐）：5分钟后用正确密码登录，应成功
   - **改数据库**：`UPDATE admin_info SET status='active', failed_attempts=0, lock_until=NULL WHERE username='senior_admin'`

### 手动-2：MySQL 不可用时健康检查（HC-002）

1. 停止 MySQL 服务
2. 调用 `GET /health`
3. 预期：503，`dependencies.mysql=DOWN`
4. 恢复 MySQL

### 手动-3：超长用户名不崩溃（BND-005）

1. 在登录页或 API 中输入 100+ 字符的用户名
2. 预期：系统不崩溃，返回 401 或前端拒绝

### 手动-4：SQL 注入测试（SEC-007）

在登录用户名输入框中尝试：`' OR '1'='1`、`'; DROP TABLE admin_info; --`
预期：不执行注入，返回 401 或空结果

### 手动-5：XSS 防护测试（SEC-008）

在搜索框输入 `<script>alert(1)</script>`
预期：前端渲染时转义为纯文本，不弹窗

### 手动-6：前端测试（FE-001 ~ FE-018）

需要前端 `npm run dev` 启动后，参照测试模板逐项验证页面功能。

---

## TRADE 联调后的测试步骤

当 TRADE 组确认接口可用后，按以下顺序测试：

1. 确认 TRADE `/health` 可访问
2. 确认 ADMIN `/health` 返回 `trade=UP`
3. 运行 `pytest test_suite.py -v -m trade`
4. 逐项验证测试结果中的 STOCK / LIMIT / CTRL / TD 部分
5. 填写 `测试结果.md` 中对应表格

---

## 总结：自动化测试覆盖清单

| # | 测试项 | 自动化 | 状态 |
|---|--------|--------|------|
| 1 | `/health` 返回正常 | ✅ | `TestHealth` |
| 2 | 四种角色正常登录 | ✅ | `TestAuthLogin` |
| 3 | 输错密码返回 401 | ✅ | `TestAuthLogin` |
| 4 | 连续5次锁定 | ✅ | `TestAuthLogin` |
| 5 | 禁用账号无法登录 | ✅ | `TestAuthLogin` |
| 6 | 无 token 返回 401 | ✅ | `TestAuthToken` |
| 7 | `/auth/me` 返回用户信息 | ✅ | `TestAuthMe` |
| 8 | 修改密码 + 旧 token 失效 | ✅ | `TestAuthToken` |
| 9 | 密码各种错误码 | ✅ | `TestAuthPassword` |
| 10 | sys_admin 查看/修改权限 | ✅ | `TestPermissions` |
| 11 | normal_admin 无权访问权限 | ✅ | `TestPermissions` |
| 12 | audit_admin 查看日志 | ✅ | `TestAudit` |
| 13 | 日志筛选功能 | ✅ | `TestAudit` |
| 14 | CSV 导出 | ✅ | `TestAudit` |
| 15 | non_audit 无权访问审计 | ✅ | `TestAudit` |
| 16 | 日志删除 | ✅ | `TestAudit` |
| 17 | 密码 bcrypt 哈希 | ✅ | `TestDatabase` |
| 18 | 外键约束 | ✅ | `TestDatabase` |
| — | 边界值(密码长度/强度) | ✅ | `TestBoundaryAuth` |
| — | 边界值(失败次数) | ✅ | `TestBoundaryAuth` |
| — | 锁定到期自动解锁 | ❌ 手动 | 见手动-1 |
| — | MySQL 不可用 | ❌ 手动 | 见手动-2 |
| — | TRADE 依赖测试 | ✅ 就绪 | `pytest -m trade` |
| — | 前端测试 | ❌ | 需前端启动 |
| — | E2E 测试 | ❌ | 需全部联调 |