# 设计矛盾与待确认事项

本文档记录在编写系统设计报告过程中发现的文档间不一致、设计疑点及待确认事项。每项问题给出**描述**、**影响**及**初步方案**，便于团队讨论与最终决策。

> 文档说明：
>
> - **A 部分**（1–10）为初次梳理时记录的跨文档不一致与设计疑点，已给出初步方案。
> - **B 部分**（11–22）为系统设计报告审计阶段新发现的问题。

---

## A. 跨文档不一致与设计疑点

### 1. 模块数量不一致

**来源**：需求分析报告 vs 接口文档 vs 设计讨论

**描述**：

- 需求分析报告定义了 7 个模块：登录管理、股票查看、涨跌停设置、交易控制、密码管理、权限管理、审计。
- 接口文档（接口V2.md 第 7.6/7.7 节）明确规定了 ADMIN 负责交易日开始/结束的接口调用（`POST /api/v1/trade/trading-days/open` 和 `/close`）。
- 设计讨论中新增了"交易日管理"功能，由管理系统发起信号、TRADE 执行。

**影响**：需要在设计报告中新增 **交易日管理模块**，或将其并入交易控制模块。

**初步方案**：✅ 已采纳

- 在设计报告中新增 **M5 交易日管理模块**（独立模块，与交易控制并列）；
- 模块编号调整：M1 登录管理、M2 股票查看、M3 涨跌停设置、M4 交易控制、**M5 交易日管理（新增）**、M6 密码管理、M7 权限管理、M8 审计。
- 模块功能限制：仅高级管理员（SENIOR_ADMIN）可访问 M5。
- *遗留问题*：需求分析报告需在下一版本同步新增此模块及对应功能编号（建议编号 tsmg070-080）。

---

### 2. 批量操作接口缺失

**来源**：需求分析报告 3.2/3.4 节 vs 接口文档第 10 节

**描述**：

- 需求分析报告定义了"批量设置"涨跌停（tsmg020-2）和"批量暂停"交易（tsmg030-2）。
- 接口文档中仅定义了单只股票的操作端点：
  - `PUT /stocks/{stock_code}/limits`（单只）
  - `POST /stocks/{stock_code}/pause`（单只）
  - `POST /stocks/{stock_code}/resume`（单只）

**影响**：批量操作在前端需循环调用单只接口，或需要 ADMIN 后端提供批量端点（内部循环调用 TRADE）。

**初步方案**：⚠️ 部分采纳

- **第一版**：ADMIN 后端提供批量入口端点（如 `POST /api/v1/admin/stocks/batch/pause`），内部使用 `asyncio.gather` 并发循环调用 TRADE 单只接口。
- **失败处理**：批量操作部分失败时，返回成功/失败明细，不回滚已成功的操作（TRADE 不支持事务性批量）。
- **性能优化**：批量数量上限 50 只，超过则前端分批提交。
- **后续版本**：建议向 TRADE 提议新增批量端点（V3 接口文档）。

---

### 3. 涨跌停生效时间规则

**来源**：需求分析报告 vs 接口文档

**描述**：

- 需求分析报告用例 3（设置涨跌停限制）中描述："系统记录设置并提示'新限制将于次日生效'"。
- 接口文档中 `effective_date` 字段可自由指定生效日期，未强制限定"次日"。

**影响**：若 `effective_date` 允许设为当日，可能改变系统行为。

**初步方案**：✅ 采用需求分析规则

- ADMIN 前端 `effective_date` 默认填充次日；后端校验 `effective_date >= today + 1`，否则返回 `ADMIN_VALIDATION_ERROR`。
- 当日已开盘后不允许修改当日涨跌停（TRADE 拒收）。
- 此规则在设计报告 4.2 节"涨跌停设置执行逻辑"中已体现。
- *待确认*：与 TRADE 组沟通，将该约束写入 V3 接口文档。

---

### 4. 验证码登录功能

**来源**：需求分析报告 3.2.1 节 vs 接口文档第 10.1 节

**描述**：

- 需求分析报告功能列表中定义了二级功能"验证码登录"（tsmg001-2）。
- 接口文档 10.1 管理员登录接口的请求体中包含 `captcha` 字段（4位验证码），但未单独定义"验证码登录"端点。

**影响**：需确认"验证码登录"是指登录表单中含验证码字段（图形验证码），还是指短信验证码登录等独立方式。

**初步方案**：✅ 按图形验证码理解

- 实现 **图形验证码（CAPTCHA）**：登录界面右侧显示验证码图片，4 位数字，由后端用 `captcha` 库生成。
- 后端将验证码答案与 session/redis key 关联，TTL 5 分钟。
- 登录请求带 `captcha` 字段，后端校验通过后销毁验证码（一次性）。
- 不实现短信验证码登录（避免外部依赖）。

---

### 5. 管理员角色枚举命名

**来源**：需求分析报告 vs 接口文档

**描述**：

- 需求分析报告使用数字枚举：0=普通管理员, 1=高级管理员, 2=系统管理员, 3=审计管理员。
- 接口文档使用字符串枚举：`NORMAL_ADMIN`, `SENIOR_ADMIN`, `SYSTEM_ADMIN`, `AUDIT_ADMIN`。

**影响**：数据库字段类型、API 返回值格式、前端展示需要约定。

**初步方案**：✅ 分层映射

- **数据库**：`role_type` 字段使用 `TINYINT(1)`，存储 0/1/2/3。
- **代码层**：定义 Python `Enum` 类，例如：

  ```python
  class AdminRole(IntEnum):
      NORMAL_ADMIN = 0
      SENIOR_ADMIN = 1
      SYSTEM_ADMIN = 2
      AUDIT_ADMIN = 3
  ```

- **API 层**：响应返回字符串名称（`NORMAL_ADMIN` 等），符合接口文档约定；请求时也接受字符串。
- **前端**：使用字符串名称展示，本地化映射为中文。

---

### 6. 客户端广播机制归属

**来源**：需求分析报告 3.2.4 节 vs 接口文档第 8.2 节

**描述**：

- 需求分析报告用例 4 中描述"向所有相关客户端广播交易重启消息"，暗示管理端参与广播。
- 接口文档明确 TRADE 通过 WebSocket（`ws://<trade-host>/api/v1/trade/ws/market`）推送 `stock.paused` / `stock.resumed` 事件。

**影响**：广播由 TRADE 执行，ADMIN 不直接向客户端推送消息。

**初步方案**：✅ 已采纳：广播职责归 TRADE

- ADMIN 仅调用 TRADE 的暂停/重启接口，**不直接向客户端推送消息**。
- TRADE 收到指令后通过 WebSocket 向所有订阅方（CLIENT、INFO 等）广播 `stock.paused` / `stock.resumed` 事件。
- ADMIN 不需要维护 WebSocket 服务端，无 NotificationService 类（设计报告类图中已移除）。
- *遗留*：需求分析报告 CRC 卡中仍有 `NotificationService`，建议在下一版本删除或改为说明性注释。

---

### 7. 数据库存储 vs 中央交易系统数据

**来源**：需求分析报告第 6 章 vs 接口文档第 4 章

**描述**：

- 需求分析报告定义了 D1（管理员信息库）、D2（操作日志库）、D3（权限配置库）三个本地数据存储。
- 接口文档第 4 章定义了共享数据模型（Stock, MarketQuote, TradeOrder, FundAccount 等），这些数据均存储在对应组的数据库中，ADMIN 通过 API 获取。

**影响**：设计报告的数据库设计应仅包含 ADMIN 本地数据库，不包含其他组的共享数据。

**初步方案**：✅ 已采纳

- ADMIN 本地数据库 `admin_db` 仅包含 4 张表：`admin_info`、`operation_log`、`permission_config`、`login_log`。
- 股票行情、订单簿等数据通过 TRADE API 实时获取，不本地缓存（或仅做短期 5 秒缓存）。
- 操作日志中记录"操作上下文"（如涨跌停设置时记录 `limit_up_ratio`、`limit_down_ratio` 和 TRADE 返回的最终价格限制），便于审计追溯，但不持久化股票主数据。

---

### 8. WebSocket 连接管理

**来源**：接口文档第 8.2 节 & 设计讨论

**描述**：

- TRADE 提供 WebSocket 行情推送，但接口文档注明 "第一版联调采用 INFO 每 5 秒 REST 拉取"。
- ADMIN 是否在第一版使用 WebSocket 或也采用 REST 轮询，未明确。

**初步方案**：✅ 第一版采用 REST 轮询

- ADMIN 第一版**仅使用 REST 接口**：每 5 秒拉取 TRADE `/market?stock_codes=` 获取授权范围内的股票行情。
- 股票状态变更（暂停/重启）通过下一次 REST 拉取感知（最多 5 秒延迟，可接受）。
- WebSocket 作为后续优化方案：使用 `websockets` Python 库，实现指数退避重连（详见 B-12）。

---

### 9. 交易日管理模块的权限归属

**来源**：设计讨论

**描述**：交易日开始/结束是高风险操作，权限归属未明确。

**初步方案**：✅ 仅高级管理员

- **决策**：仅 SENIOR_ADMIN（高级管理员）有权调用交易日开始/结束接口。
- **理由**：交易日管理与交易控制（暂停/重启）紧密相关，权限应一致；系统管理员负责账号管理，不参与交易业务。
- **审计**：所有交易日开始/结束操作必须记录到 `operation_log`，operation_type=`TRADING_DAY`。
- **自动化**：交易日结束可由后端定时任务自动触发（每日 15:00），但仍需记录为"系统自动操作"，operator_admin_id 标记为 `SYSTEM`。
- *待确认*：是否需要"二次确认"防止误操作（已在 UI 设计中加入二次确认弹窗）。

---

### 10. 接口文档版本

**来源**：接口V2.md 标题

**描述**：当前接口文档为 v0.2，属于草稿阶段，后续可能变更。

**初步方案**：✅ 跟踪机制

- 设计报告 1.5 节"参考资料"中已注明接口文档版本。
- 每次接口文档更新后，本设计报告同步更新外部接口章节（3.7 节）。
- 在 Git 提交记录中明确接口文档版本号变更。
- 建立接口变更通知机制（建议在小组群发布更新公告）。

---

## B. 设计报告审计阶段新发现的问题

### 11. 模块编号变更对需求-设计追溯性的影响

**来源**：审计设计报告 3.3 节 vs 需求分析报告 3.2 节

**描述**：

- 需求分析报告中模块编号：1.登录管理 → 7.审计（共 7 个）。
- 设计报告因新增"交易日管理"，编号变为：M1 登录管理 → M8 审计。
- 功能编号 `tsmg050`（审计）原本对应需求第 7 模块，现对应设计 M8 模块。

**影响**：需求-设计之间的追溯关系出现"编号偏移"，可能导致测试用例追溯困难。

**初步方案**：

- 在设计报告 3.3 节模块表中**显式标注**每个模块对应的需求功能编号范围（如 M1 ↔ tsmg001-tsmg004，M8 ↔ tsmg050-tsmg052）。
- 在设计报告中明确"M5 交易日管理为新增模块，需求分析报告下一版本将补充对应功能编号"。
- 制作 **需求-设计追溯矩阵**（独立文档），列出每个需求功能项对应的设计模块、接口、数据库表。

---

### 12. JWT 令牌撤销与无状态特性的冲突

**来源**：审计设计报告 4.2 节"密码管理执行逻辑"

**描述**：

- 设计报告 line 647 写道："将当前 JWT 令牌加入黑名单（或直接要求前端清除），强制重新登录"。
- JWT 的核心特性是无状态，加入黑名单需要 Redis/数据库存储，破坏无状态优势。
- 如果仅要求前端清除，攻击者保留旧令牌后仍可使用至过期。

**影响**：安全机制不严谨，密码修改/账号禁用后旧令牌仍可使用。

**初步方案**：⚠️ 两种方案任选

**方案 A**（推荐，更安全）：使用 **JWT + Redis 黑名单**

- 每个 JWT 包含 `jti`（JWT ID）字段；
- 注销/密码修改/账号禁用时，将 `jti` 加入 Redis 黑名单，TTL = JWT 剩余有效时间；
- 每次请求中间件校验 JWT 时同时检查黑名单。

**方案 B**（无 Redis）：使用 **短期 JWT + 数据库版本号**

- JWT 过期时间设为短（如 30 分钟）；
- `admin_info` 表添加 `token_version` 字段，密码修改/禁用时自增；
- JWT payload 包含 `token_version`，中间件每次请求都查 DB 校验版本号。
- 缺点：每次请求都要查 DB，性能略差。

**当前决策**：采用方案 A（引入 Redis 作为黑名单 + 验证码存储）。

---

### 13. 登录失败锁定阈值的边界条件

**来源**：审计设计报告 4.2 节 vs 数据库设计 6.2 节

**描述**：

- 4.2 节："失败则 `failed_attempts += 1`，**若达 5 次则将 `status` 设为 `locked`**"。
- 6.2 节 `failed_attempts` 字段说明："默认 0，范围 0-5"。
- 边界不清：是 `failed_attempts == 5` 触发锁定，还是 `failed_attempts >= 5`？范围应是 0-4 还是 0-5？

**影响**：实现时可能出现"第 5 次失败时是否还显示错误信息再锁定"的歧义。

**初步方案**：✅ 明确语义

- 规则：第 5 次失败时同时锁定账号，即"第 5 次失败也算锁定生效"。
- 实现：
  ```python
  failed_attempts += 1
  if failed_attempts >= 5:
      status = 'locked'
      lock_until = now() + timedelta(minutes=5)
      return "账号已被锁定，请 5 分钟后重试"
  else:
      return f"密码错误，您还有 {5 - failed_attempts} 次机会"
  ```
- 字段范围更新为 0-5（5 表示已达到锁定阈值并已锁定）。
- 解锁后 `failed_attempts` 清零。

---

### 14. 数据库表外键删除策略未明确

**来源**：审计数据库设计 6.2 节

**描述**：

- `operation_log.admin_id` 外键引用 `admin_info(admin_id)`。
- `login_log.admin_id`、`permission_config.admin_id` 同理。
- 未明确 `ON DELETE` 和 `ON UPDATE` 行为。
- 设计上管理员账号"禁用"而非"删除"，但仍需明确规则。

**影响**：若意外执行 DELETE，可能导致日志孤儿记录或级联删除。

**初步方案**：

- 全部外键设置为 `ON DELETE RESTRICT ON UPDATE CASCADE`。
- 业务上**禁止物理删除管理员账号**，仅允许 `status = 'disabled'`。
- 在 DAO 层封装：禁用 SQLAlchemy 的 `session.delete(admin)` 直接删除管理员对象。
- 数据归档时（如 6 个月后）才考虑物理清理，需先归档关联日志。

---

### 15. 状态字段 status 类型选择

**来源**：审计数据库设计 6.2 节

**描述**：

- `admin_info.status` 使用 `VARCHAR(10)` 存储 active / locked / disabled。
- 字符串存储占用空间较大，可读性好但效率低。

**影响**：性能轻微影响，可读性优秀。

**初步方案**：⚠️ 保留 VARCHAR（次要优化）

- 保留 `VARCHAR(10)`，理由：
  - 接口文档约定 `status` 为字符串枚举（与角色枚举的处理方式一致）；
  - 表数据量小（最多几十条管理员记录），性能影响可忽略；
  - 阅读 SQL 时直观。
- 备选优化：使用 MySQL 8.0 ENUM 类型（更省空间，但 ORM 兼容性需测试）。

---

### 16. ADMIN 服务缺少 /health 端点

**来源**：审计设计报告 3.7 节 vs 接口V2.md 第 13 节

**描述**：

- 接口V2.md 第 13 节 "联调要求"：每组服务必须提供 `/health` 端点。
- 设计报告 3.7 节"外部接口（ADMIN 对外提供的 API）"未列出 `/health`。

**影响**：联调阶段健康检查无法进行。

**初步方案**：✅ 补充

在设计报告 3.7 节 ADMIN 对外 API 表格中追加：

| 方法 | 端点      | 描述                                          |
|------|-----------|-----------------------------------------------|
| GET  | /health   | 健康检查，返回 `{"service": "admin-service", "status": "UP", "time": "..."}` |
| GET  | /docs     | FastAPI 自动生成的 Swagger UI                |
| GET  | /openapi.json | OpenAPI JSON schema                       |

注：`/health`、`/docs`、`/openapi.json` 通常不带 `/api/v1/admin` 前缀。

---

### 17. ACCOUNT 调用关系冗余

**来源**：审计设计报告 2.4 节 / 3.7 节

**描述**：

- 设计报告 2.4 节："本子系统可调用账户业务子系统（ACCOUNT）查询管理员关联的投资者账户信息（如需）"。
- 实际场景：管理员**不是投资者**，不应有资金/证券账户。
- 接口文档第 6 节 ACCOUNT 接口均围绕投资者账户，与管理员业务无关。

**影响**：错误的依赖描述会误导开发与测试，且 ACCOUNT 组的接口设计未考虑 ADMIN 这一调用方。

**初步方案**：✅ 删除 ACCOUNT 依赖

- 删除设计报告中对 ACCOUNT 的调用描述；
- 保留 ACCOUNT 在"项目背景"中作为兄弟子系统的介绍即可；
- 类图中删除 `AccountClient` 类；
- 部署图中保留 ACCOUNT 节点但去掉 ADMIN ↔ ACCOUNT 的连线。

---

### 18. WebSocket 断线重连机制未设计

**来源**：审计设计报告 vs 接口V2.md 第 8.3 节

**描述**：

- 接口V2.md 8.3 节："客户端断线后应指数退避重连"。
- 设计报告未提及 WebSocket 断线重连，仅说"REST 轮询作为第一版"。

**影响**：若后续启用 WebSocket，无重连机制将导致行情数据中断。

**初步方案**：

- 第一版采用 REST 轮询（详见 A-8），暂不实现 WebSocket，因此暂无影响。
- 后续启用 WebSocket 时，在 `TradeClient.WebSocketManager` 中实现：
  - 30 秒心跳（ping / pong）；
  - 断线后按 `1s, 2s, 4s, 8s, 16s, 30s, 30s, ...` 指数退避重连；
  - 最大重连次数 10 次，超过后告警；
  - 重连成功后重新订阅之前的 stock_codes。
- 此设计在第 8 章"系统出错设计"中补充。

---

### 19. 审计日志查询：操作日志与登录日志合并 vs 分离

**来源**：审计设计报告 vs 接口V2.md 第 10.9 节

**描述**：

- 设计报告 4.1 节描述"查询 `operation_log` 与 `login_log` 表"，暗示一个查询接口返回两表数据。
- 接口V2.md 10.9 节只定义了 `GET /audit/logs?...&operation_type=...`，未区分操作日志与登录日志。

**影响**：API 设计上是合并查询还是分离？返回结构如何统一？

**初步方案**：✅ 分离两个端点

- 提供两个独立端点：
  - `GET /api/v1/admin/audit/operation-logs` 查询操作日志；
  - `GET /api/v1/admin/audit/login-logs` 查询登录日志。
- 在设计报告 3.7 节 API 表格中明确列出两个端点。
- 接口文档 V3 中也建议拆分。
- 前端审计界面提供 Tab 切换。

---

### 20. 登录验证码实现细节未明确

**来源**：审计设计报告 4.2 节

**描述**：

- 设计报告 4.2 节："后端先在 Redis/内存中校验验证码正确性（如使用）"。
- "如使用" 表述模糊，未明确实施细节。

**影响**：开发时无明确指引，可能各人实现方式不同。

**初步方案**：✅ 明确实施细则（已在 A-4 中部分给出）

- 后端使用 `captcha` Python 库生成 4 位数字图片验证码；
- 验证码键：`captcha:{session_id}` 或 `captcha:{client_fingerprint}`，存于 Redis；
- TTL = 5 分钟；
- 登录请求需带 `captcha_id`（前端首次请求验证码时由后端返回）+ `captcha` 答案；
- 校验后立即销毁（一次性），无论登录成功失败；
- 验证码图片端点：`GET /api/v1/admin/auth/captcha` 返回 `{"captcha_id": "...", "image": "data:image/png;base64,..."}`。
- 在设计报告 3.7 节补充此端点。

---

### 21. 数据库实例隔离策略未明确

**来源**：审计设计报告 3.5 节 vs 接口V2.md 第 1.1 节

**描述**：

- 接口V2.md："各组可以各自维护独立数据库 schema，也可以共用同一 MySQL 实例并通过表名前缀或数据库名隔离"。
- 设计报告说"MySQL（admin_db）"，但未明确是独立实例还是共用实例。

**影响**：影响部署架构、资源配置与运维方案。

**初步方案**：

- **第一版（联调）**：共用同一 MySQL 实例，按数据库名隔离（`admin_db` 与 TRADE 组的 `trade_db` 同实例不同 db）。
  - 优势：节省服务器资源，便于本地开发；
  - 风险：性能耦合，互相影响。
- **正式上线**：独立 MySQL 实例，提供更好的故障隔离。
- 部署图（3.5 节）中明确标注"第一版共用实例"。

---

### 22. 日志归档表设计缺失

**来源**：审计设计报告 7 章"系统维护" vs 6 章"数据库设计"

**描述**：

- 7.3 节："超过 30 天的日志归档至历史表或导出文件"。
- 6 章数据库设计中未定义归档表（如 `operation_log_archive`）。

**影响**：归档机制无对应的表结构，实施时缺乏依据。

**初步方案**：✅ 补充归档表

- 新增表 `operation_log_archive` 与 `login_log_archive`，结构与主表完全一致；
- 区别：
  - 归档表无外键约束（允许引用已删除/归档的 admin_id）；
  - 归档表使用按月分区（`PARTITION BY RANGE (YEAR(operation_time)*100 + MONTH(operation_time))`）；
  - 索引只保留 `operation_time`、`admin_id`（去除写入侧索引以减小存储）。
- 归档策略：
  - 每月 1 日凌晨定时任务执行：将 31 天前的数据从主表 INSERT INTO 归档表，再 DELETE FROM 主表；
  - 归档表保留 6 个月（180 天），超期数据导出为 CSV/Parquet 文件到对象存储；
  - 审计模块支持查询历史日志（自动 UNION 主表与归档表）。
- 此设计应补充到设计报告 6.2 节末尾。

---

## C. 后续行动项

| 优先级 | 行动项 | 责任 |
|--------|--------|------|
| 高 | 设计报告补充 `/health` 端点说明（B-16） | 文档维护 |
| 高 | 设计报告补充审计日志拆分为两端点（B-19） | 文档维护 |
| 高 | 设计报告删除 ACCOUNT 调用关系（B-17） | 文档维护 |
| 中 | 与 TRADE 组确认 V3 接口补充批量端点（A-2） | 组长 |
| 中 | 与 TRADE 组确认涨跌停 `effective_date` 校验规则（A-3） | 组长 |
| 中 | 设计报告补充验证码图片端点（B-20） | 文档维护 |
| 中 | 设计报告补充日志归档表设计（B-22） | 文档维护 |
| 低 | 需求分析报告下一版本补充交易日管理模块（A-1, B-11） | 需求维护 |
| 低 | 建立需求-设计追溯矩阵（B-11） | 测试组 |

---

*最后更新：2026-05-26*
