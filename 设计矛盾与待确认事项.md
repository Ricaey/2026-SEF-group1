# 设计矛盾与待确认事项

本文档记录在编写系统设计报告过程中发现的文档间不一致、设计疑点及待确认事项。

---

## 1. 模块数量不一致

**来源**：需求分析报告 vs 接口文档 vs 设计讨论

**描述**：
- 需求分析报告定义了 7 个模块：登录管理、股票查看、涨跌停设置、交易控制、密码管理、权限管理、审计。
- 接口文档（接口V2.md 第 7.6/7.7 节）明确规定了 ADMIN 负责交易日开始/结束的接口调用（`POST /api/v1/trade/trading-days/open` 和 `/close`）。
- 设计讨论中新增了"交易日管理"功能，由管理系统发起信号、TRADE 执行。

**影响**：需要在设计报告中新增 **交易日管理模块**，或将其并入交易控制模块。

**建议**：新增为独立模块 M8（交易日管理模块），由高级管理员操作。

---

## 2. 批量操作接口缺失

**来源**：需求分析报告 3.2/3.4 节 vs 接口文档第 10 节

**描述**：
- 需求分析报告定义了"批量设置"涨跌停（tsmg020-2）和"批量暂停"交易（tsmg030-2）。
- 接口文档中仅定义了单只股票的操作端点：
  - `PUT /stocks/{stock_code}/limits`（单只）
  - `POST /stocks/{stock_code}/pause`（单只）
  - `POST /stocks/{stock_code}/resume`（单只）

**影响**：批量操作在前端需循环调用单只接口，或需要 ADMIN 后端提供批量端点（内部循环调用 TRADE）。

**建议**：暂定前端循环调用单只接口，后续如需性能优化再由 ADMIN 后端提供批量接口。

---

## 3. 涨跌停生效时间规则

**来源**：需求分析报告 vs 接口文档

**描述**：
- 需求分析报告用例 3（设置涨跌停限制）中描述："系统记录设置并提示'新限制将于次日生效'"。
- 接口文档中 `effective_date` 字段可自由指定生效日期，未强制限定"次日"。

**影响**：若 `effective_date` 允许设为当日，可能改变系统行为。

**建议**：按需求分析报告的规则实施——`effective_date` 必须为次日或之后；当日已开盘后不允许修改当日涨跌停。此约束建议在 V3 接口文档中明确。

---

## 4. 验证码登录功能

**来源**：需求分析报告 3.2.1 节 vs 接口文档第 10.1 节

**描述**：
- 需求分析报告功能列表中定义了二级功能"验证码登录"（tsmg001-2）。
- 接口文档 10.1 管理员登录接口的请求体中包含 `captcha` 字段（4位验证码），但未单独定义"验证码登录"端点。

**影响**：需确认"验证码登录"是指登录表单中含验证码字段（图形验证码），还是指短信验证码登录等独立方式。

**建议**：设计阶段暂按"登录表单包含图形验证码"理解，`captcha` 字段为 4 位数字验证码。

---

## 5. 管理员角色枚举命名

**来源**：需求分析报告 vs 接口文档

**描述**：
- 需求分析报告使用数字枚举：0=普通管理员, 1=高级管理员, 2=系统管理员, 3=审计管理员。
- 接口文档使用字符串枚举：`NORMAL_ADMIN`, `SENIOR_ADMIN`, `SYSTEM_ADMIN`, `AUDIT_ADMIN`。
- 代码中使用字符串常量更可读，但数据库存储整数更高效。

**建议**：数据库存储整数（TINYINT），代码层面使用字符串枚举，API 层按接口文档使用字符串。建立映射关系。

---

## 6. 客户端广播机制归属

**来源**：需求分析报告 3.2.4 节 vs 接口文档第 8.2 节

**描述**：
- 需求分析报告用例 4 中描述"向所有相关客户端广播交易重启消息"，暗示管理端参与广播。
- 接口文档明确 TRADE 通过 WebSocket（`ws://<trade-host>/api/v1/trade/ws/market`）推送 `stock.paused` / `stock.resumed` 事件。

**影响**：广播由 TRADE 执行，ADMIN 不直接向客户端推送消息。ADMIN 仅负责调用 TRADE 接口。

**建议**：设计报告中明确：ADMIN 调用 TRADE 暂停/重启接口 → TRADE 执行操作并通过 WebSocket 广播。ADMIN 不承担广播职责。

---

## 7. 数据库存储 vs 中央交易系统数据

**来源**：需求分析报告第 6 章 vs 接口文档第 4 章

**描述**：
- 需求分析报告定义了 D1（管理员信息库）、D2（操作日志库）、D3（权限配置库）三个本地数据存储。
- 接口文档第 4 章定义了共享数据模型（Stock, MarketQuote, TradeOrder, FundAccount 等），这些数据均存储在对应组的数据库中，ADMIN 通过 API 获取。
- ADMIN 不存储股票行情数据、交易指令、资金账户信息。

**影响**：设计报告中的数据库设计应仅包含 ADMIN 本地数据库（管理员、操作日志、权限配置、登录日志），不包含其他组的共享数据。

**建议**：已在模板中按此原则设计。但需要确认 D2 操作日志库中是否需要存储 TRADE 返回的详细信息（如涨跌停最终计算价格）。

---

## 8. WebSocket 连接管理

**来源**：接口文档第 8.2 节 & 设计讨论

**描述**：
- TRADE 提供 WebSocket 行情推送（`ws://<trade-host>/api/v1/trade/ws/market`），ADMIN 可订阅以获取实时行情和股票状态变更。
- 接口文档注明："第一版联调采用 INFO 每 5 秒 REST 拉取"，WebSocket "保留为后续优化方案"。
- ADMIN 是否在第一版使用 WebSocket 或也采用 REST 轮询，未明确。

**建议**：ADMIN 第一版采用与 INFO 相同的策略：每 5 秒 REST 拉取 TRADE `/market` 接口。WebSocket 作为后续优化。

---

## 9. 交易日管理模块的权限归属

**来源**：设计讨论

**描述**：
- 交易日开始/结束是高风险操作。谁有权执行？
- 需求分析报告中高级管理员具备交易控制权限，但未明确提及交易日管理。

**建议**：
- 交易日开始：高级管理员（SENIOR_ADMIN）或系统管理员（SYSTEM_ADMIN）。
- 交易日结束：高级管理员手动触发，或系统定时任务自动触发。
- 建议仅高级管理员拥有交易日管理权限，因为这与交易控制紧密相关。

---

## 10. 接口文档版本

**来源**：接口V2.md 标题

**描述**：当前接口文档为 v0.2，属于草稿阶段。后续接口变更可能影响设计。

**建议**：跟踪接口文档更新，设计报告中的接口设计章节与最新接口文档保持一致。

---

*最后更新：2026-05-25*
